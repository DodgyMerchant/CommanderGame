/// @func scr_zoom_value(value,optimize);
/// @desc seeks the zoom with the given value
/// @arg {real} value value to seek
/// @arg {real} optimize if the zoom to seek should be optimized to be of best ratio/resolution

/*
argument0	value
argument1	optimize

*/

#region optimizing

if argument1
	{
	var _val=argument0;
	
	
	
	
	
	}

#endregion


zoom_seek=clamp(_val,0,1);


