/// @func scr_draw_color(color);
/// @desc checks if color is being drawn and if not it will se it
/// @arg {real} color color to check for and set


if draw_get_color()!=argument0
	draw_set_color(argument0);

