/// @func scr_zoom_height(height,optimize);
/// @desc seeks the zoom with the given height
/// @arg {real} height height to seek
/// @arg {real} optimize if the zoom to seek should be optimized to be of best ratio/resolution

/*
argument0	height
argument1	optimize

*/






var _val = argument0;

scr_zoom_value(_val,argument1);