/// @func scr_zoom_width(width,optimize);
/// @desc seeks the zoom with the given width
/// @arg {real} width width to seek
/// @arg {real} optimize if the zoom to seek should be optimized to be of best ratio/resolution

/*
argument0	width
argument1	optimize

*/








var _val = argument0;

scr_zoom_value(_val,argument1);